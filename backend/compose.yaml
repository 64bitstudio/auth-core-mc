services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=auth_core_mc'
      - 'POSTGRES_USER=auth_core_mc'
      - 'POSTGRES_PASSWORD=auth_core_mc_dev'
    ports:
      - '5432'
  redis:
    image: 'redis:7-alpine'
    ports:
      - '6379'
